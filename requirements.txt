# ── 핵심 (주보·PPT 기능) — 이것만 있어도 PPT/주보는 완전 동작 ──
Flask>=3.0
python-pptx>=1.0
pyinstaller>=6.0

# ── 설교 영상 기능 (선택) ─────────────────────────────────────────
# 아래는 영상 → 쇼츠/요약 기능에만 필요. 미설치 시 해당 탭만 비활성.
# 추가로 시스템에 ffmpeg/ffprobe 실행파일이 설치되어 있어야 합니다.
#   pip install -r requirements-video.txt  로 따로 설치할 수도 있습니다.
ffmpeg-python>=0.2.0
faster-whisper>=1.1.0
fpdf2>=2.8.0
openai>=1.40.0
google-genai>=1.0.0
slack-sdk>=3.27.0
python-dotenv>=1.0.0
