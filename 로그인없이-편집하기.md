# 🖥️ 로그인 없이 내 컴퓨터에서 주보 만들기

> **로그인도, 인터넷 설정도 필요 없습니다.** 내 컴퓨터에서 폼으로 주보를 만들고 저장하는 방법입니다.
> (Sveltia CMS 의 "Work with Local Repository" 기능 — 크롬(Chrome) 또는 엣지(Edge) 브라우저 권장)

---

## 준비 — 딱 한 번만

1. 이 폴더를 컴퓨터에 내려받습니다. (GitHub 저장소 → `Code ▾ → Download ZIP` → 압축 풀기)
2. `Node.js` 를 설치합니다. (`주보 시작하기` 를 처음 더블클릭하면 설치 안내가 자동으로 뜹니다)

---

## 매번 하는 순서 (5단계)

### 1. 켜기
`주보 시작하기` 파일을 더블클릭합니다. (Windows `.bat` / Mac `.command`)
→ 검은 창이 뜨고 잠시 뒤 브라우저에 홈페이지가 열립니다. **이 창은 닫지 마세요.**

### 2. 편집 화면 열기
`주보 만들기` 파일을 더블클릭합니다.
→ 브라우저에 **Sveltia CMS** 화면이 열립니다.

### 3. 내 폴더 연결 (처음 한 번만)
화면의 파란 버튼 **"Work with Local Repository"** 를 누릅니다.
→ 폴더 선택 창이 뜨면, **압축을 푼 이 폴더(seraphim)** 를 선택하고 **"허용/열기"** 를 누릅니다.
> 💡 다음부터는 대개 이 단계를 건너뛰고 바로 폼이 열립니다.

### 4. 주보 작성
- 왼쪽 **"주보"** → **"새로 만들기(＋)"** 를 누릅니다.
- **날짜** 를 고르고, **설교 제목 · 본문 말씀 · 설교자**(필수 3개)를 채웁니다.
- 공지·일정·인사말 등은 있으면 쓰고 없으면 비워둡니다. (비우면 홈페이지에 안 나옴)
- 오른쪽 **미리보기**로 실제 모습을 확인합니다.

### 5. 저장
오른쪽 위 **"저장(Save)"** 을 누릅니다.
→ `src/bulletins/` 폴더에 **날짜 이름의 파일로 바로 저장**됩니다.
→ 홈페이지(브라우저)를 새로고침하면 **방금 만든 주보가 최신으로 자동 표시**됩니다.

**끝! 매주 2~5단계만 반복하면 됩니다.**

---

## 자주 묻는 것

**Q. "Work with Local Repository" 버튼이 안 보여요.**
→ 크롬 또는 엣지 브라우저를 쓰세요. (사파리·구형 브라우저는 이 기능을 지원하지 않습니다)

**Q. 저장했는데 홈페이지에 안 보여요.**
→ 브라우저에서 **새로고침(F5)** 하세요. 그래도 없으면 `주보 시작하기` 창이 켜져 있는지 확인하세요.

**Q. 계좌 등 민감정보는요?**
→ 헌금 항목의 '공개 범위'를 **"교인만"** 으로 두면 비밀번호를 넣어야 보입니다. 비밀번호는
   `src/_data/church.json` 의 `memberPassword`(기본 `1234` — 꼭 바꾸세요).

---

## 만든 주보를 인터넷에 올려 카톡으로 나누려면?

이 방식(로컬 편집)은 **내 컴퓨터 안**에서만 보입니다. 교인들과 카톡으로 나누려면 한 번의
"인터넷에 올리기"가 필요합니다. 두 가지 방법 중 하나를 쓰세요.

- **간단**: 컴퓨터를 아는 분(집사님·청년 등)에게 저장 후 `git push` 를 부탁 → 1~2분 뒤 사이트 자동 갱신.
- **자동화(원하면 만들어 드립니다)**: `주보 인터넷에 올리기` 더블클릭 파일을 추가하면 저장분을
  자동으로 올릴 수 있습니다. 필요하시면 요청하세요.

> 이미 공개된 우리 교회 사이트: **https://hyotowork-create.github.io/Seraphim/**

---

_더 자세한 내용: [`시작하기.md`](시작하기.md) · [`README.md`](README.md)_
