{
  "name": "sveltia-cms-auth",
  "version": "1.0.0",
  "private": true,
  "description": "교회 주보 사이트 /admin 용 GitHub OAuth 중계 워커 (Cloudflare Workers)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
