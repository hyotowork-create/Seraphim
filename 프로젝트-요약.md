# 교회 주보 홈페이지 — 프로젝트 요약 & 핸드오프 문서

> 이 문서 하나면 프로젝트 전체를 이해하고 Claude Cowork/Claude Code 에서 바로 이어서 작업할 수 있습니다.
> 대상: 소형·미자립 교회 목회자(비개발자·무예산·폰 중심). 목표: 무료로 매주 주보를 웹에 올리고 카톡으로 공유.

---

## 1. 현재 상태 (한눈에)

| 항목 | 상태 |
|---|---|
| **라이브 사이트** | 🟢 배포됨 → **https://hyotowork-create.github.io/Seraphim/** |
| 저장소 | `hyotowork-create/Seraphim` (public) |
| 기본 브랜치 | `main` (작업 브랜치 `claude/church-bulletin-site-k3Wo7` 와 동일 내용) |
| 자동 배포 | 🟢 GitHub Actions (`main` push → 1~2분 뒤 자동 갱신) |
| Phase 1 (MVP) | ✅ 완료 |
| Phase 2 (CMS 로그인 발행) | ✅ 코드/워커 완료 · ⏳ 워커 배포·OAuth 앱은 사용자 계정 작업 필요 |
| 오프라인 실행 | ✅ 완료 (공개 페이지 외부요청 0, /admin 로컬 번들) |
| 데스크탑 더블클릭 실행 | ✅ 완료 (`주보 시작하기` / `주보 만들기`) |
| Phase 3 (OCR·통계 등) | ⬜ 미착수 |

**공개 주소**
- 홈: https://hyotowork-create.github.io/Seraphim/
- 주보 상세: https://hyotowork-create.github.io/Seraphim/bulletin/2026-06-07/
- 지난 주보: https://hyotowork-create.github.io/Seraphim/archive/
- 관리자(편집): https://hyotowork-create.github.io/Seraphim/admin/

---

## 2. 무엇을 만드는가 (제품 개요)

목회자가 폼에 그 주 내용을 채우면 → 그 주 홈페이지가 자동 생성 → 링크를 카톡으로 공유하는 무료 정적 웹앱.

**설계 원칙**
1. 무료 유지(호스팅·도구 전부 무료). 2. 운영 부담 0(서버·DB 없음, 정적).
3. 폰에서 끝난다. 4. 고령 교인 가독성(큰 글씨·단일 컬럼·고대비·큰 버튼).
5. 카톡 유통 최적화(OG 태그로 썸네일). 6. 실수 방지(미리보기·공개범위 통제).

**확정 스택**: Eleventy(11ty) + Nunjucks · Sveltia CMS(모바일·Git 기반) · Sveltia CMS Auth(Cloudflare Workers) · GitHub Pages · GitHub 저장소 1개.

---

## 3. 저장소 구조

```
Seraphim/
├─ .github/workflows/deploy.yml     # GitHub Pages 자동 빌드·배포 (main push 트리거)
├─ .eleventy.js                     # Eleventy 설정 (필터: dateSlug/dateKorean/absUrl/sha256, bulletins 컬렉션)
├─ .gitattributes                   # .bat=CRLF, .command=LF 줄바꿈 고정
├─ package.json / package-lock.json # 의존성 (@11ty/eleventy ^3)
├─ src/
│  ├─ _data/church.json             # 교회 고정정보(이름·주소·예배시간·계좌·memberPassword·url·defaultImage)
│  ├─ _includes/
│  │  ├─ layout.njk                 # 공통 레이아웃 + OG/트위터 카드 메타
│  │  ├─ bulletin.njk               # 주보 상세 레이아웃(매크로 호출)
│  │  └─ partials/bulletin-body.njk # 주보 본문 매크로(홈·상세 공용)
│  ├─ bulletins/
│  │  ├─ bulletins.11tydata.js      # 주보 폴더 공통설정(permalink/tags + eleventyComputed: pageTitle/desc/ogImage/shareUrl)
│  │  ├─ 2026-06-07.md              # 샘플 주보 1
│  │  └─ 2026-05-31.md              # 샘플 주보 2
│  ├─ index.njk / index.11tydata.js # 홈(최신 주보 자동노출) + 홈 OG 계산
│  ├─ archive.njk                   # 지난 주보 목록 + 검색
│  ├─ assets/
│  │  ├─ style.css                  # 모바일우선·고대비·큰버튼 + 인쇄용 CSS + [hidden]{display:none!important}
│  │  ├─ bulletin.js                # 공유 버튼(Web Share/링크복사) + 헌금정보 가림막(SHA-256 비교)
│  │  ├─ archive.js                 # 아카이브 검색(제목/설교자/구절)
│  │  └─ og-default.svg             # 기본 공유 썸네일
│  └─ admin/
│     ├─ index.html                 # Sveltia CMS 로더(→ ./vendor/sveltia-cms.js, 오프라인)
│     ├─ config.yml                 # CMS 폼 정의(=데이터 모델). backend.repo/base_url/site_url 수정 필요
│     └─ vendor/sveltia-cms.js      # @sveltia/cms 0.170.0 번들(오프라인용, 1.9MB)
├─ auth-worker/                     # /admin GitHub 로그인 중계 워커(Cloudflare)
│  ├─ src/index.js                  # OAuth: /auth(리다이렉트+CSRF쿠키) · /callback(토큰교환→postMessage)
│  ├─ wrangler.toml / package.json
│  └─ README.md                     # 워커 배포 가이드(OAuth App·시크릿)
├─ README.md                        # 설치·기술 설명(개발자용)
├─ 시작하기.md                       # 초보자용 쉬운 사용 설명서(더블클릭 실행 최상단)
├─ 주보 시작하기.bat / .command      # 더블클릭 실행: Node확인→준비→서버→브라우저
└─ 주보 만들기.bat / .command        # 더블클릭: 편집화면(/admin) 열기
```

---

## 4. 데이터 모델 (주보 1부 = 파일 1개 `src/bulletins/YYYY-MM-DD.md`)

front matter(YAML). ✱ = 필수.

```yaml
date        # ✱ 예배일(파일·URL 키). 날짜만 정하면 /bulletin/YYYY-MM-DD/ 자동 생성
season      # 절기(대림절 등), 비우면 미표시
serviceType # 주일예배/수요예배 등
sermon:
  title      # ✱ 설교 제목
  scripture  # ✱ 본문 성경구절
  preacher   # ✱ 설교자
  summary    # 설교 요약/전문 (여러 줄)
  videoUrl   # 유튜브 등 영상 링크
worshipOrder            # 예배 순서
announcements: [{title, body}]   # 공지 여러 개
schedule:      [{day, content}]  # 주간 일정
bulletinImage           # 주보 원본 이미지(공유 썸네일로도 쓰임)
offering: { accountInfo, visibility }  # 헌금 계좌 + 공개범위(public/members/hidden)
greeting                # 교역자 인사말
```
⚠️ 콜론(`:`)이나 괄호가 든 값(계좌 등)은 반드시 `"큰따옴표"` 로 감싸기.

**고정값 자동채움**: 매주 같은 값(교회명·주소·예배시간·기본계좌·비밀번호)은 `src/_data/church.json` 에 한 번만.

---

## 5. 페이지 & 기능

| URL | 내용 |
|---|---|
| `/` | 가장 최근 주보 자동 노출 |
| `/bulletin/YYYY-MM-DD/` | 날짜별 상세. og:title/description/image/url 포함. 하단 "카톡·링크 공유"·"인쇄" 버튼 |
| `/archive/` | 지난 주보 목록 + 제목/설교자/구절 즉시 검색 |
| `/admin/` | Sveltia CMS 입력 화면(폼) |

- **공유**: Web Share API(폰의 카톡 포함) → 실패 시 링크 클립보드 복사.
- **인쇄**: 인쇄용 CSS로 네비·버튼 숨기고 내용만 깔끔히.
- **민감정보 공개범위**: `public`(전체) / `members`(비밀번호 입력 후 표시, SHA-256 해시 비교로 원문 비노출) / `hidden`(미표시). 기본은 보수적으로 `members` 권장. 비밀번호는 `church.json` 의 `memberPassword`(기본 `1234` — 반드시 변경).

---

## 6. 사용법

### 6-A. 매주 하는 일 (목회자, 폼으로)
1. 브라우저에서 사이트 주소 뒤에 `/admin` → 2. GitHub 로그인(처음 1회) → 3. "새로 만들기" 후 날짜 선택 + 필수 3개(제목·본문·설교자) 채우기 → 4. 미리보기로 확인(개인정보 공개범위 점검) → 5. 발행 → 6. 새 주보 페이지에서 "카톡·링크 공유".

### 6-B. 데스크탑에서 더블클릭 실행 (터미널 불필요)
1. 저장소 ZIP 다운로드 → 압축풀기 → (1회) Node.js 설치(런처가 없으면 설치 페이지 자동 안내).
2. `주보 시작하기`(Win `.bat`/Mac `.command`) 더블클릭 → 서버 실행 + 브라우저 자동 열림.
3. `주보 만들기` 더블클릭 → `/admin` 편집화면. **"Work with Local Repository"** 로 폴더 선택 시 인터넷·로그인 없이 로컬 파일 직접 편집·저장.

### 6-C. 오프라인 실행
- 공개 페이지는 외부 요청 0으로 완전 동작. `/admin` 도 로컬 번들(`vendor/sveltia-cms.js`)로 렌더.
- 편집은 6-B의 "Work with Local Repository"(크로미움 계열 브라우저).
- 편집 화면의 아이콘·글꼴은 Google Fonts라 오프라인에선 시스템 글꼴로 대체(기능 영향 없음).

### 6-D. 개발자용(로컬 미리보기)
```bash
npm install
npm start        # http://localhost:8080  (watch 자동 리빌드)
npm run build    # 정적 산출물 _site/
```

---

## 7. 처음 준비(1회) & 배포

### 이미 완료됨
- GitHub Pages: **Settings → Pages → Source = GitHub Actions** (설정 완료).
- `main` push 시 `.github/workflows/deploy.yml` 이 `npm ci` → build(PATH_PREFIX 자동 `/저장소이름/`) → 아티팩트 업로드 → deploy-pages 로 배포.

### 남은 선택 작업
1. **church.json 실제값 입력** (권장): `url` → `"https://hyotowork-create.github.io"`, 교회명·주소·계좌·`memberPassword` 변경. → 카톡 썸네일/공유 링크 정확해짐.
2. **/admin 온라인 로그인 발행**(Phase 2 마무리): `auth-worker/README.md` 대로
   - GitHub OAuth App 생성(callback = `https://<워커>/callback`)
   - `cd auth-worker && npx wrangler login && npm run deploy`
   - `wrangler secret put GITHUB_CLIENT_ID / GITHUB_CLIENT_SECRET (/ ALLOWED_DOMAINS)` 후 재배포
   - `src/admin/config.yml` 의 `backend.repo` / `backend.base_url` / `site_url` 을 실제값으로 → push.

---

## 8. 검증 결과 (이번 세션에서 실제 실행)

- **빌드/CI**: `npm ci` + build 재현 성공, 산출물 5종 생성. GitHub Actions build+deploy 잡 모두 ✅.
- **화면(폰 390px)**: 홈/상세/아카이브 렌더 정상(스크린샷). 단일컬럼·큰글씨·고대비 확인.
- **인터랙션**: 헌금 가림막(1234→계좌공개 / 오답→오류·숨김), 아카이브 검색("빌립"→1건) ✅.
- **OG 태그**: og:title/description/image/url + 하위경로(/Seraphim/) 정확.
- **인쇄 CSS**: 인쇄 미디어에서 네비·버튼 숨김, A4 PDF 정상.
- **인증 워커**: 로컬 실행 13/13 통과(302 리다이렉트·CSRF·도메인차단·토큰교환·success/error postMessage).
- **내부 링크**: 16개 무결성 0 오류.
- **오프라인**: 외부 네트워크 차단 상태에서 공개 3페이지 외부요청 0, /admin 로컬번들 렌더 확인.
- **버그 수정 1건**: `[hidden]` 속성이 `.protected-form{display:flex}`에 덮여 무효화 → `[hidden]{display:none!important}` 로 해결.

---

## 9. 다음 작업 아이디어 (Phase 3 & 개선)

- **주보 이미지 OCR 자동 채움**: 주보 사진 업로드 → 텍스트 추출 → 폼 초안 자동 입력.
  - 1순위 Naver CLOVA OCR(한국어 정확도 높음, 월 무료 할당) / 대안 Google Vision·Tesseract.js.
- 설교 검색·태그(성경별·주제별), 지난 설교 모아보기, 간단 방문 통계.
- **OG 기본 이미지 PNG화**: 카카오톡 SVG 썸네일 미지원 대비 1200×630 PNG/JPG 로 교체(`church.json.defaultImage`).
- **/admin 완전 오프라인화(선택)**: Google Fonts·Material Symbols 아이콘까지 번들 내장.
- Windows 아이콘(.ico)·바탕화면 바로가기, "주보 인터넷에 올리기"(git push 대행) 더블클릭 파일.

---

## 10. Claude Cowork 에서 이어서 작업할 때 팁

- 저장소를 열고 이 문서(`프로젝트-요약.md`)와 `README.md`, `시작하기.md`, `auth-worker/README.md` 를 먼저 참고.
- 코드 변경 후 `npm run build` 로 확인, `main` 에 push 하면 자동 배포.
- 주보 추가는 `src/bulletins/YYYY-MM-DD.md` 파일 하나 추가(4장 데이터 모델 참고).
- 흔한 함정: YAML 콜론/괄호 값은 따옴표, 이미지·링크 경로는 `| url` 필터 사용(하위경로 자동 보정), 절대 URL은 `absUrl` 필터.

---

_생성: 프로젝트 핸드오프용 요약. 라이브: https://hyotowork-create.github.io/Seraphim/_
